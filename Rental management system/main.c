#include"Menu.c"

int main(int argc, char const *argv[])
{
    Menu();
    
    return 0;
}

    // struct tm* t;
    // time_t now;
    // time(&now);
    // t = localtime(&now);
    // printf("%d年%d月%d日\n", t->tm_year+1900, t->tm_mon+1, t->tm_mday);